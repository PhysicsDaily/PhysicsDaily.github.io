// This placeholder page is intentionally excluded from the build.
// The canonical route is /mechanics/measurements (index.js).

export async function getStaticProps() {
  return { notFound: true };
}

export default function MeasurementsNew() {
  return null;
}