---
layout: base.njk
---

<div class="chapter-container">
    <div class="chapter-header">
        <div class="container">
            <div class="breadcrumb">
                <a href="/">Home</a>
                <span class="separator">›</span>
                <a href="/{{ topic }}">{{ topic | topicName }}</a>
                <span class="separator">›</span>
                <span class="current">Chapter {{ chapter }}: {{ title }}</span>
            </div>
            <h1>{{ title }}</h1>
            <p class="chapter-subtitle">{{ description }}</p>
        </div>
    </div>

    <div class="chapter-navigation">
        <div class="container">
            <div class="nav-cards">
                {# Removed conceptual.html link #}
                
                {% if hasMCQ %}
                <a href="mcq.html" class="nav-card">
                    <span class="nav-icon">📊</span>
                    <span class="nav-title">Multiple Choice Quiz</span>
                    <span class="nav-description">Practice with MCQs</span>
                </a>
                {% endif %}
                
                {# Removed numerical.html link #}
                
                {% if hasSimulation %}
                <a href="simulation.html" class="nav-card">
                    <span class="nav-icon">🔬</span>
                    <span class="nav-title">Interactive Simulation</span>
                    <span class="nav-description">Visualize concepts</span>
                </a>
                {% endif %}
            </div>
        </div>
    </div>

    <main class="chapter-content">
        <div class="container">
            {{ content | safe }}
            
            {% if learningObjectives %}
            <section class="learning-objectives">
                <h2>🎯 Learning Objectives</h2>
                <ul>
                    {% for objective in learningObjectives %}
                    <li>{{ objective }}</li>
                    {% endfor %}
                </ul>
            </section>
            {% endif %}
            
            {% if keyConcepts %}
            <section class="key-concepts">
                <h2>🔑 Key Concepts</h2>
                <div class="concepts-grid">
                    {% for concept in keyConcepts %}
                    <div class="concept-card">
                        <h3>{{ concept.title }}</h3>
                        <p>{{ concept.description }}</p>
                        {% if concept.formula %}
                        <div class="concept-formula">{{ concept.formula }}</div>
                        {% endif %}
                    </div>
                    {% endfor %}
                </div>
            </section>
            {% endif %}
        </div>
    </main>
</div>
