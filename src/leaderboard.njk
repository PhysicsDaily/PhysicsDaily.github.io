---
layout: layouts/base.njk
title: "Leaderboard"
description: "Daily, weekly and monthly XP leaderboard for Physics Daily learners."
permalink: leaderboard.html
skipMainWrapper: true
headExtra: |
  <style>
  :root { --lb-accent: var(--primary-color, #2563eb); }
  .lb-hero { background: var(--bg-secondary); border-bottom: 1px solid var(--border-color); padding: 3rem 0; }
  .lb-hero h1 { font-size: clamp(1.75rem, 3vw, 2.5rem); margin: 0 0 .5rem; }
  .lb-hero p { color: var(--text-secondary); }

  .lb-tabs { display: flex; gap: .5rem; margin: 1rem 0 1.5rem; }
  .lb-tab { border: 1px solid var(--border-color); background: var(--bg-primary); color: var(--text-primary); padding: .5rem .9rem; border-radius: 10px; cursor: pointer; font-weight: 600; transition: all .2s ease; }
  .lb-tab:hover { border-color: var(--primary-color); color: var(--primary-color); }
  .lb-tab.active { background: var(--lb-accent); color: #fff; border-color: var(--lb-accent); box-shadow: 0 4px 10px rgba(37, 99, 235, 0.25); }

  .leaderboard-card { background: var(--card-bg); border: 1px solid var(--border-color); border-radius: 14px; box-shadow: var(--shadow-sm, 0 1px 2px rgba(0,0,0,.05)); overflow: hidden; }
  .leaderboard-header { display: flex; align-items: center; justify-content: space-between; padding: 1rem 1.25rem; border-bottom: 1px solid var(--border-color); gap: 1rem; flex-wrap: wrap; }
  .leaderboard-title { font-size: 1.1rem; font-weight: 700; }
  .leaderboard-subtitle { color: var(--text-secondary); font-size: .9rem; }

  .lb-self { display:flex; align-items:center; gap:.5rem; }
  .lb-self .xp-pill { display:inline-flex; align-items:center; gap:.4rem; background: var(--bg-tertiary); border:1px solid var(--border-color); color: var(--text-primary); padding:.35rem .6rem; border-radius:999px; font-weight:700; }
  .lb-self .xp-pill .xp-icon { width:18px; height:18px; display:inline-flex; align-items:center; justify-content:center; background:linear-gradient(180deg,#e2e8f0,#cbd5e1); color:#334155; border-radius:6px; font-size:.7rem; }

  .lb-meta { padding: .75rem 1.25rem .25rem; color: var(--text-secondary); display:flex; align-items:center; justify-content:space-between; gap:1rem; flex-wrap:wrap; }
  .lb-reset { font-size:.9rem; }
  .lb-rankbar { width:100%; padding: .25rem 1.25rem 1rem; }
  .lb-rankbar .zones { position:relative; height:8px; border-radius:999px; background: linear-gradient(90deg, var(--primary-color), var(--primary-color)); opacity:.3; border:1px solid var(--border-color); }
  .lb-rankbar .pointer { position:absolute; top:50%; transform: translate(-50%, -50%); width:16px; height:16px; border-radius:50%; background:var(--primary-color); border:2px solid white; box-shadow: 0 2px 8px rgba(0,0,0,.15); }
  .lb-rankbar .rank-value { font-weight:700; color: var(--text-primary); }

  .table-wrapper { overflow-x: auto; }
  .leaderboard-table { width: 100%; border-collapse: collapse; min-width: 520px; }
  .leaderboard-table th, .leaderboard-table td { padding: 1rem; border-bottom: 1px solid var(--border-color); text-align: left; }
  .leaderboard-table th { color: var(--text-secondary); font-weight: 600; font-size: .9rem; text-transform: uppercase; letter-spacing: .5px; }
  .leaderboard-table tr { transition: all .2s ease; }
  .leaderboard-table tr:hover { background: var(--bg-tertiary); transform: translateY(-1px); box-shadow: 0 2px 8px rgba(0,0,0,.05); }
  .leaderboard-table tr.me { background: linear-gradient(90deg, rgba(37,99,235,0.1), transparent); border-left: 3px solid var(--primary-color); }

  .rank { width: 80px; font-weight: 800; color: var(--text-primary); position: relative; }
  .rank-medal { display: inline-flex; align-items: center; justify-content: center; width: 32px; height: 32px; border-radius: 50%; font-size: 1.1rem; font-weight: 800; }
  .rank-1 { background: linear-gradient(135deg, #ffd700, #ffed4e); color: #92400e; }
  .rank-2 { background: linear-gradient(135deg, #c0c0c0, #e5e7eb); color: #374151; }
  .rank-3 { background: linear-gradient(135deg, #cd7f32, #d97706); color: white; }
  .rank-other { background: var(--bg-tertiary); color: var(--text-primary); border: 1px solid var(--border-color); }

  .name .name-cell { display: flex; align-items: center; gap: .8rem; }
  .name .flag { font-size: 1.3rem; filter: drop-shadow(0 1px 2px rgba(0,0,0,.1)); }
  .name .text { font-weight: 600; }
  .country { color: var(--text-secondary); font-family: 'JetBrains Mono', ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, 'Liberation Mono', 'Courier New', monospace; font-size: .85rem; text-transform: uppercase; letter-spacing: .5px; }
  .xp { font-weight: 700; color: var(--primary-color); font-size: 1.1rem; }
  .xp::after { content: ' XP'; font-size: .8rem; color: var(--text-secondary); font-weight: 500; }

  .lb-footer-note { padding: .9rem 1.25rem; color: var(--text-secondary); font-size: .9rem; }

  #leaderboardLoading { display: none; align-items: center; justify-content: center; padding: 2rem; color: var(--text-secondary); }
  #leaderboardEmpty { display: none; padding: 2rem; color: var(--text-secondary); text-align: center; }
  #leaderboardError { display:none; margin: 1rem 1.25rem; padding: .75rem 1rem; border-radius: 10px; border: 1px solid var(--danger-color, #ef4444); color: var(--danger-color, #ef4444); background: rgba(239,68,68,.08); }

  @media (max-width: 768px) {
      .leaderboard-header { justify-content: flex-start; }
      .lb-tabs { flex-wrap: wrap; }
  }
  </style>
pageScripts:
  - src: "/assets/js/leaderboard.js"
    defer: true
---
<header class="lb-hero">
  <div class="container">
    <h1>Leaderboard</h1>
    <p>See who’s climbing the Physics Daily ranks. Sign in to appear on the board.</p>
  </div>
</header>

<main class="main-content">
  <div class="container" id="leaderboardApp">
    <div class="lb-tabs">
      <button id="tab-daily" class="lb-tab active">Daily</button>
      <button id="tab-weekly" class="lb-tab">Weekly</button>
      <button id="tab-monthly" class="lb-tab">Monthly</button>
    </div>

    <section class="leaderboard-card">
      <div class="leaderboard-header">
        <div class="leaderboard-title">Top Learners</div>
        <div class="leaderboard-subtitle" id="leaderboardSubtitle">Last 24 hours</div>
        <div class="lb-self">
          <div class="xp-pill" title="Your XP in this board">
            <span class="xp-icon">XP</span>
            <span id="lbSelfXp">0</span>
          </div>
        </div>
      </div>
      <div class="lb-meta">
        <div class="lb-reset" id="lbResetInfo">Resets soon</div>
        <div class="rank-value" id="lbRankValue">Rank: —</div>
      </div>
      <div class="lb-rankbar">
        <div class="zones" id="lbRankZones">
          <div class="pointer" id="lbRankPointer" style="left: 100%"></div>
        </div>
      </div>
      <div id="leaderboardError"></div>
      <div id="leaderboardLoading">Loading…</div>
      <div id="leaderboardEmpty">No XP activity yet. Answer section checks and take quizzes to earn XP!</div>
      <div class="table-wrapper">
        <table class="leaderboard-table">
          <thead>
            <tr>
              <th>#</th>
              <th>Name</th>
              <th>Country</th>
              <th>XP</th>
            </tr>
          </thead>
          <tbody id="leaderboardTableBody"></tbody>
        </table>
      </div>
      <div class="lb-footer-note">XP is earned by answering section Quick Checks correctly and by quiz performance. Tie-breakers are resolved by most recent activity.</div>
    </section>
  </div>
</main>
---